const Header = async () => {
    return (
        <header className="border-grid bg-background/95 supports-backdrop-filter:bg-background/60 fixed top-0 z-50 w-full border-b backdrop-blur-sm">
            <div className="container mx-auto flex h-14 max-w-[1200px] items-center px-10"></div>
        </header>
    );
};

export default Header;
